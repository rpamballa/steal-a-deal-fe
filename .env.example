# Base URL of the StealADeal backend API.
# Leave unset for local development (defaults to http://localhost:8282).
# Set per environment in CI / cloud secrets — never hardcode in source.
VITE_API_BASE_URL=http://localhost:8282
